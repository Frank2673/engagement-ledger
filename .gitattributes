* text=auto eol=lf

*.mjs text eol=lf
*.js  text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.md  text eol=lf

# Windows 用户直接双击运行的脚本保留 CRLF
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 审计日志与锚定文件：逐行追加，且必须是字节稳定的 ——
# 任何行尾自动转换都会改变内容，进而让哈希链对不上
*.jsonl -text
ANCHORS.txt -text

# 授权书原件是二进制/不可转换内容
*.pdf binary
*.docx binary
*.png  binary
