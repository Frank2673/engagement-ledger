# 安全模型与威胁分析

本工具的安全目标很特殊：**它自己就是被攻击对象**。
一个记录了越界行为的审计日志，最有动机篡改它的人正是那个越界的人。
因此这里逐条列出攻击者能做什么、本工具能发现什么、以及**发现不了什么**。

诚信声明：下面「能发现」的每一条都在 `tests/ledger.test.mjs` 与
`.github/workflows/ci.yml` 的 `tamper-detection` 作业里有对应的负向验证；
「不能发现」的每一条也都写了为什么，而不是含糊带过。

---

## 一、攻击面

### 1.1 篡改日志文件

| 攻击手法 | 能否发现 | 依据 |
| --- | --- | --- |
| 改动某条记录的内容（把越界目标改成范围内的） | ✅ 能 | 该条哈希与内容不符，校验在精确位置失败 |
| 改动某条记录的时间戳 | ✅ 能 | 时间戳在哈希覆盖范围内 |
| 删掉中间某条记录 | ✅ 能 | `seq` 不连续 |
| 交换两条记录顺序 | ✅ 能 | `prevHash` 断链 |
| 删掉**末尾**若干条记录 | ⚠️ 单靠链不能 | 截断后的链本身自洽 —— **必须靠外部锚定**（见 §三） |
| 把整条链重算一遍（无 HMAC） | ⚠️ 单靠链不能 | 算法公开，知道算法就能重算 —— 靠外部锚定 |
| 把整条链重算一遍（有 HMAC，攻击者无密钥） | ✅ 能 | 无密钥算不出正确的 HMAC |
| 用错误的密钥校验 | ✅ 能 | 报告密钥不匹配，不静默通过 |
| 不带密钥去校验 HMAC 链 | ✅ 能 | 校验失败，而非误判通过 |

**结论**：哈希链提供的是 **tamper-evident（篡改可发现）**，不是 tamper-proof（不可伪造）。
把它当成"改不了"来宣传是行业里常见的过度承诺，本工具不做这个承诺。

### 1.2 伪造整份日志

攻击者可以删掉现有日志，重新生成一份"干净"的、内容完全自洽的新链。

- **纯哈希链**：完全挡不住。**这一条有实测**：掩改一条越界记录、把整条链从头重算之后，
  `verify` 仍然报「哈希链完整」—— 单看日志看不出任何问题。
- **HMAC**：攻击者没有密钥，无法生成能通过校验的新链。但如果他能**读到**旧日志，
  他仍然可以整份删除（可用性问题，不是完整性问题）。
- **外部锚定**：真正的解法。把链头哈希提交进 git 后，攻击者要么改写 git 历史
  （在有远端与多人协作时几乎必然暴露），要么让新链的哈希与已锚定的对不上。

**关键是必须有校验端**：只"写锚定"而不"验锚定"，等于没锚定。
`verify-anchor` 逐条比对历史锚定点，实测结果：

| 攻击手法 | `verify`（只看日志） | `verify-anchor`（比对锚定） |
| --- | --- | --- |
| 掩改一条记录 + 整链重算 | ✅ 通过（链条自洽） | ❌ `rewritten`：链头与锚定冲突 |
| 砍掉末尾若干条（回滚） | ✅ 通过（截断后仍自洽） | ❌ `rollback`：锚定条数 > 当前条数 |
| 只改内容不重算 | ❌ 检出（链断裂） | ❌ `broken` |

这两种攻击在 CI 里每次都会真跑一遍（`scripts/verify.mjs` 的 B9 / B10），
不是纸面推演。

### 1.3 在工具之外动手

**这是本工具最大的盲区，且无法通过技术手段解决。**
审计日志只能证明"已记录的内容未被改动"，不能证明"所有动作都被记录了"。

- 使用者可以在本工具之外直接对目标发起请求，日志里不会有任何痕迹。
- 缓解办法不在代码里，而在流程里：要求操作方在**每次动作前**调用 `check`，
  并把日志作为交付物的一部分提交；配合客户侧的网络日志交叉核对。
  **本工具的定位是"让守规矩的人能自证"，不是"防止不守规矩的人作弊"。**
  这个区别必须在交付时向客户讲清楚，否则会造成虚假的安全感。

### 1.4 凭证被换

| 攻击手法 | 能否发现 | 依据 |
| --- | --- | --- |
| 换掉授权书原件，扩大范围 | ✅ 能 | 凭证登记了授权书 SHA-256，`init` 时比对 |
| **把时间窗改窄**（让已做的事变成越窗） | ⚠️ 部分能 | 报告 §4.1 会列出落在当前窗口之外的已执行动作 |
| **把时间窗改宽**（让曾越窗的事看起来合规） | ❌ 不能 | 见下 |
| 改掉凭证里的范围后重新 init | ⚠️ 需流程 | 见下 |
| 直接手改凭证 JSON | ⚠️ 需流程 | 凭证本身不在哈希链保护范围内 |

**为什么"改宽窗口"发现不了**：被校验门拦下的动作有记录（`decision=denied`），
但如果操作方在窗口外**绕过本工具**直接动手，日志里不会留下任何东西，
事后把窗口改宽也无从对证 —— 这属于 §1.3，技术上无解。

反过来，"改窄窗口"是可以发现的：已执行的动作带着真实时间戳留在链上，
一旦落在当前窗口之外，报告第 4.1 节就会把它们列出来。

**凭证自身不设签名**是刻意的取舍：加入凭证签名会让"改个范围"变得麻烦，
而现实中范围变更是常态（客户临时扩大或缩小）。取而代之的做法是：

- 每次范围变更都记录一条 `note` 类型的日志条目，说明谁在何时因何变更；
- 凭证文件本身应当提交进一个**独立**的仓库或受保护的存储；
- 报告里完整列出凭证内容，让授权方能核对"这就是我签的那份范围"；
- 报告里同时列出越窗动作（§4.1）—— 这样"窗口被动过"至少留下一个可查的痕迹。

### 1.5 密钥与凭据泄露

- HMAC 密钥**只从环境变量读取**（`ENGAGEMENT_LEDGER_KEY`），
  不接受命令行参数 —— 命令行参数在 `ps` / 任务管理器里对同机其他用户可见。
  这条约束由 `scripts/check-zero-deps.mjs` 在 CI 里强制。
- 密钥不写入日志文件、不写入报告、不出现在任何输出中。
- 日志与报告默认被 `.gitignore` 排除（含客户资产清单）。

### 1.6 输入注入

日志的来源字段有：目标名、动作名、结果、证据指针。这些都来自命令行输入，
最终会被渲染进 Markdown 报告的表格里。

| 手法 | 处理 |
| --- | --- |
| 目标名含 `\|` 撑破表格列 | ✅ 每个单元格都做转义（含表头以外的全部单元格） |
| 目标名含换行 | ✅ 换行被替换为空格 |
| 动作名含 Markdown 语法 | ✅ 包在反引号内且转义 |

`scripts/check-report-tables.mjs` 在 CI 里对生成的报告做列数一致性校验；
该脚本自身也有负向测试（`tests/tables.test.mjs`），确保它能真的发现问题。

### 1.7 资源耗尽

日志是逐行追加读取的（`loadLedger` 一次性读入内存）。百万级条目的大日志会占用相应内存。
对本工具的预期用途（单次委托几千到几万条动作记录）不构成问题；
如果要在超大日志上运行，应改为流式校验。

### 1.8 日志混入 / 委托归属错误

**这不是攻击，而是一种更常见的失败：把两次委托的记录混成一份报告交给客户。**

| 情形 | 处理 |
| --- | --- |
| 两次委托共用同一个 `--ledger` 路径 | ✅ `log` 比对 genesis 的委托编号与当前凭证，不符即拒绝追加 |
| 交接时把两份日志合并了 | ✅ `verify` / `status` / `report` 检出并定位到具体条目 |
| 由旧版本或外部工具写入的记录 | ✅ 同上 —— 检查是读时做的，不依赖写入方配合 |
| 记录未标注委托编号 | ⚠️ 只提示，不判为异常（无法自动确认归属，但也无法否定） |

**为什么链完整性挡不住它**：混入的记录同样在哈希链上，链条完全自洽。
「内容没被改」与「内容都属于这次委托」是两个独立的命题，
前者由哈希链回答，后者必须单独校验。

检出后 `report` 返回退出码 3，并在第 5.1 节列出外来条目及其实际所属委托，
同时写明「统计数字与动作流水包含了不属于本次委托的内容，不可直接交付客户」。

**能力边界**：该检查保证「已记录的内容在委托归属上自洽」，
但发现不了「真的在两个目标上做了事却只在日志里写了一个委托」——
后者属于 §1.3（在工具之外动手），技术上无解，只能靠客户侧日志交叉核对。

**这个检查本身也可能失效**：如果操作方在混入之后重新 `init` 了一份日志
（把 genesis 换成自己的编号），混入的记录就成了「自己的」。
这类操作会让链头哈希变化，因此**必须靠外部锚定**（§三）来发现 ——
锚定行里的条数与哈希对不上，就说明日志被重建过。

---

## 二、不在威胁模型内的事

- **防范有 root 权限的对手**：能改文件系统的人总能让校验脚本本身失效。
  对抗这种对手需要外部锚定 + 独立取证，不是本工具的目标。
- **防范篡改 Node 运行时**：同上。
- **验证授权书的法律效力**：本工具只做技术层面的哈希比对。
- **网络层的窃听与中间人**：本工具不联网（零依赖、无网络请求）。

---

## 三、部署建议（按威胁等级）

| 场景 | 最低要求 |
| --- | --- |
| 内部自查 / 学习 | 纯哈希链 + 每天 `anchor --append ANCHORS.txt` 并提交进 git |
| 客户交付 | 上述 + HMAC 密钥（由客户或第三方持有更佳）+ 交付前 `verify-anchor` 留档 |
| 受监管行业 / 有争议风险 | 上述 + 锚定行提交进**客户可见的**仓库或可信时间戳服务 + 客户侧网络日志交叉核对 |

**关键一句（两条，缺一条都白做）**：

1. `anchor` 的输出必须落到日志文件之外。只把链头哈希打印在同一个终端里，等于没锚定 ——
   攻击者重算整链时也会顺带重算那个哈希。
2. **必须有 `verify-anchor` 这一步**。锚定只是把哈希存到别处；只有拿它回来比对，
   才能发现"整链被重写"。没有校验端的锚定文件，只是一份没人看的文本。

建议把这两条写进交付流程：交付前跑一次 `verify-anchor`，把输出（连同退出码）一起留档。

---

## 四、报告安全问题

如果你在使用中发现安全问题（尤其是"本工具声称能发现问题、实际不能"这类
**虚假保证**——那比缺失功能更严重），请通过以下方式报告：

- 开一个 Issue，标注 `security`；若涉及细节不便公开，先开 Issue 说明"有一个安全问题，
  需要私下沟通渠道"。

请一并提供：复现步骤、实际输出、期望输出。**不需要**提供任何真实客户信息 ——
用一个虚构的凭证即可复现本工具的全部逻辑。
