# 使用边界与伦理声明

engagement-ledger 是一个**合规工具**，不是攻击工具。它不发起任何网络请求、
不扫描任何目标、不包含任何漏洞利用代码。它做的是三件事：

1. 在动作发生**之前**，判定这次动作是否落在书面授权范围内；
2. 把判定依据与结果写进一份改不了的日志；
3. 在委托结束时，把日志变成一份可给客户和审计方看的合规报告。

## 一、这个工具存在的理由

渗透测试行业里最危险的一句话是「我有授权」。它无法被验证：

- 客户怎么知道你测的确实只有他授权的那些资产？
- 事后争议时，你怎么证明某个动作当时是被允许的？
- 授权书被换过一份范围更大的，谁能发现？

这三个问题都不是技术能力问题，而是**证据问题**。本工具针对的就是证据问题。

## 二、硬性禁止：不随配置放开

以下动作被硬编码在 `src/lib/manifest.mjs` 的 `NEVER_PERMITTED_ACTIONS` 中，
**任何凭证都无法把它们列入允许清单**，凭证校验阶段就会直接报错：

```
dos  ddos  destructive  wipe  data-exfiltration  exfiltration  ransomware
```

为什么不做成配置项：一旦它是配置项，判断权就从一个工程常量变成了一个 JSON 文件。
而那个 JSON 文件是可能被改的 —— 把「不得造成服务不可用」这条底线交给一个可改的文件，
等于没有底线。

`scripts/check-zero-deps.mjs` 会在 CI 里校验这份清单没有被削弱。

## 三、使用者的责任

本工具**不能**替你完成授权判断。它能做的是：

- ✅ 校验凭证字段是否齐全（有没有授权书编号、签署人、时间窗、范围）
- ✅ 校验授权书文件是否被换过（比对 SHA-256）
- ✅ 判定具体目标/动作/时间是否落在凭证声明的范围内
- ✅ 把判定结果固化成证据

它**不能**做的是：

- ❌ 判断那份授权书本身是否真实、是否由有权签字的人签的
- ❌ 判断签署人是否真的有权授权该资产（这个问题只有客户能回答）
- ❌ 知道你没记录在日志里的动作
- ❌ 阻止你在本工具之外直接对目标动手

**凭证里的授权范围写错了，工具会忠实地按错误范围放行。**
因此凭证必须来自真实的书面授权，且应当由授权方复核。

## 四、使用前必须确认的事

在把任何目标写进 `inScope` 之前，逐条确认：

- [ ] 我拿到了**书面**授权（邮件也算，但要有明确的授权人与日期）
- [ ] 授权书里写清了**资产清单**，而不只是「某某公司的系统」
- [ ] 授权书里写清了**起止时间**
- [ ] 授权书里写清了**允许的动作**（很多授权书只允许扫描，不允许人工验证）
- [ ] 授权书里写清了**排除项**（支付系统、生产数据库往往是排除的）
- [ ] 我知道**出事时找谁**（`emergencyContact`）
- [ ] 我确认了这些资产**属于授权方**（客户经常不知道自己的资产边界在哪）

任何一条打不上勾，就先别写 `inScope`。

## 五、绝对不做的事

无论授权书怎么写，以下行为不在本工具的支援范围内，也不应在任何真实测试中发生：

- 造成目标服务不可用（包括「测一下抗压能力」）
- 下载、复制、外传真实业务数据（证明漏洞存在只需最小片段）
- 植入后门、持久化、留取访问通道
- 对客户员工做社会工程（这需要另一份针对人的授权）
- 把测试中发现的问题卖给第三方或公开披露（除非授权书明确允许）

## 六、日志的正确用法

- **日志文件不进公开仓库**：它含客户资产清单与测试细节。`.gitignore` 已默认排除 `*.jsonl`。
- **锚定行可以进仓库**：`ANCHORS.txt` 里只有条数与哈希，不含任何客户信息，
  这正是它适合提交进 git 的原因 —— 用第三方（GitHub）的不可改写历史为你的日志背书。
- **HMAC 密钥不进日志、不进仓库、不进命令行**：通过环境变量 `ENGAGEMENT_LEDGER_KEY` 提供。
  密钥丢失意味着该链只能做"篡改可发现"级别的校验，无法证明伪造与否。

## 七、免责

本工具按 MIT 协议"原样"提供，不附带任何担保。使用者对自己的一切测试行为负责。
工具输出的报告是**自动化汇总结果**，不是法律意见；涉及法律责任时应咨询专业律师。
